---
const faqs = [
    {
        question: "What areas of Melbourne do you service?",
        answer: "We service all Melbourne suburbs, including the CBD, inner suburbs, and industrial zones.",
    },
    {
        question: "Are your cleaners insured and police checked?",
        answer: "Yes. All staff are police checked and fully insured.",
    },
    {
        question: "Do you bring your own equipment and supplies?",
        answer: "Yes. We supply all commercial-grade equipment and eco-friendly products.",
    },
    {
        question: "Do you offer after-hours cleaning?",
        answer: "Yes. Early mornings, evenings, and weekends are available.",
    },
    {
        question: "Do you provide upfront pricing or estimates?",
        answer: "Yes. We offer transparent pricing and fixed plans so businesses can estimate costs before committing.",
    },
];
---

<section class="faq-section" id="faq1">
    <div class="gcc-container">
        <div class="header">
            <h2 class="section-title">Frequently Asked Questions</h2>
            <p class="section-subtitle">Clear answers, no surprises.</p>
        </div>

        <div class="faq-grid">
            {
                faqs.map((f) => (
                    <details class="faq-item">
                        <summary class="faq-question">
                            {f.question}
                            <span class="icon">+</span>
                        </summary>
                        <div class="faq-answer">
                            <p>{f.answer}</p>
                        </div>
                    </details>
                ))
            }
        </div>
    </div>
</section>

<style>
    .faq-section {
        padding: 2rem 0; /* Compact padding */
        background: #ffffff;
    }

    .header {
        text-align: center;
        max-width: 40rem;
        margin: 0 auto 4rem;
    }

    .section-title {
        font-family: "Montserrat", sans-serif;
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--brand-dark);
        margin-bottom: 0.5rem;
    }

    .section-subtitle {
        color: var(--text-muted);
        font-size: 1.125rem;
    }

    .faq-grid {
        max-width: 48rem;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .faq-item {
        background: var(--bg-surface);
        border: 1px solid var(--border-soft);
        border-radius: 1rem;
        overflow: hidden;
        transition: all 0.3s ease;
    }

    .faq-item[open] {
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
        border-color: var(--brand-teal);
    }

    .faq-question {
        padding: 1.25rem 1.5rem;
        font-weight: 700;
        color: var(--brand-dark);
        cursor: pointer;
        display: flex;
        justify-content: space-between;
        align-items: center;
        list-style: none; /* Hide default marker */
    }

    .faq-question::-webkit-details-marker {
        display: none;
    }

    .icon {
        font-size: 1.5rem;
        font-weight: 400;
        color: var(--brand-green);
        transition: transform 0.2s ease;
    }

    details[open] .icon {
        transform: rotate(45deg);
    }

    .faq-answer {
        padding: 0 1.5rem 1.5rem;
        color: var(--text-muted);
        line-height: 1.6;
        animation: slideDown 0.2s ease-out;
    }

    @keyframes slideDown {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @media (max-width: 900px) {
        .section-title {
            font-size: 2rem;
        }

        .section-subtitle {
            font-size: 1rem;
        }

        .header {
            margin-bottom: 2.5rem;
            padding: 0 1rem;
        }

        .faq-question {
            padding: 1rem;
            font-size: 1rem;
        }
    }
</style>
